spring:
  application:
    name: transportation-service
  datasource:

    url: jdbc:mysql://localhost:3306/affable
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9000
